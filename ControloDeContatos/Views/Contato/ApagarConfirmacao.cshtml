@model ContatoModel

@{
    ViewData["Title"] = "Confirmação de apagar contato";
}

<div class="text-center">
    <h3>Realmente deseja apagr o (@Model.Nome)?</h3> 
</div>

<a class="btn btn-primary btn-block" role="button" asp-controller="Contato" asp-action="Index">Cancelar</a>
<a class="btn btn-primary btn-block" role="button" asp-controller="Contato" asp-route-id="@Model.Id" asp-action="Apagar">Confirmar exclusão</a>
